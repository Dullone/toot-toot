<div class="header">
  <% if user_signed_in? %>
      <%= link_to "sign out",  destroy_user_session_path , method: :delete %> | 
      <%= link_to "toots", user_toots_path(current_user) %>
      <% if @user != current_user %>
        follow
      <% end %>
  <% elsif controller_name != "sessions" && action_name != "new" %>
    <%= link_to "sign in",  user_session_path %>
  <% else %>
    <%= "not signed in" %>
  <% end %>
</div>